const AuthMiddleware = require('./auth')
const UserMiddleware = require('./users')
const MoodsMiddleware = require('./moods')
module.exports = {
    AuthMiddleware,
    UserMiddleware,
    MoodsMiddleware
}
